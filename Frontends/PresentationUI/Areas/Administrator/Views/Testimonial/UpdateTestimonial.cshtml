@model TestimonialViewModel

@{
    ViewData["Title"] = "UpdateTestimonial";
    Layout = "~/Areas/Administrator/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-flex align-items-center justify-content-between">
            <h4 class="mb-0 font-size-18">Referans Güncelle</h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);">Referanslarımız</a></li>
                    <li class="breadcrumb-item active">Referans Güncelle</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <form method="post">
                    <div class="row">
                        <div class="col-xl-6">
                            <div class="form-group">
                                <label for="txt-name">Adı-Soyadı</label>
                                <input class="form-control" id="txt-name" value="@Model.GetTestimonialDto.Name" asp-for="UpdateTestimonialDto.Name" placeholder="Ad-Soyad Giriniz" />
                            </div>
                        </div>
                        <div class="col-xl-6">
                            <div class="form-group">
                                <label for="txt-title">Unvan</label>
                                <input class="form-control" id="txt-title" value="@Model.GetTestimonialDto.Title" asp-for="UpdateTestimonialDto.Title" placeholder="Unvan Giriniz" />
                            </div>
                        </div>
                        <div class="col-xl-6">
                            <div class="form-group">
                                <label for="txt-image">Fotoğraf</label>
                                <input class="form-control" id="txt-image" value="@Model.GetTestimonialDto.Image" asp-for="UpdateTestimonialDto.Image" placeholder="Fotoğraf Seçiniz" />
                            </div>
                        </div>
                        <div class="col-xl-6">
                            <div class="form-group">
                                <label for="txt-comment">Yorum</label>
                                <input class="form-control" id="txt-comment" value="@Model.GetTestimonialDto.Comment" asp-for="UpdateTestimonialDto.Comment" placeholder="Yorum Giriniz" />
                            </div>
                        </div>
                    </div>
                    <input type="hidden" value="@Model.GetTestimonialDto.TestimonialID" asp-for="UpdateTestimonialDto.TestimonialID" />
                    <button type="submit" class="btn btn-primary waves-effect waves-light">Güncelle</button>
                </form>
            </div>
        </div>
    </div>
</div>

